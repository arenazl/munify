# ========================================
# CONFIGURACIÓN SMTP PARA EMAILS
# ========================================
# Copiar estas líneas al archivo .env y completar con tus credenciales

# OPCIÓN 1: Gmail (recomendado para testing)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-app-password   # NO uses tu contraseña normal, genera un "App Password"
SMTP_FROM=tu-email@gmail.com
SMTP_FROM_NAME=Sistema de Reclamos Chacabuco

# OPCIÓN 2: Mailtrap (para desarrollo/testing)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=tu-mailtrap-user
# SMTP_PASSWORD=tu-mailtrap-password
# SMTP_FROM=noreply@municipalidad.gob.ar
# SMTP_FROM_NAME=Sistema de Reclamos Municipal

# OPCIÓN 3: SendGrid (producción)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=tu-sendgrid-api-key
# SMTP_FROM=noreply@municipalidad.gob.ar
# SMTP_FROM_NAME=Municipalidad de Chacabuco

# ========================================
# CÓMO GENERAR APP PASSWORD EN GMAIL:
# ========================================
# 1. Ir a: https://myaccount.google.com/security
# 2. Habilitar "2-Step Verification" si no está activado
# 3. Ir a "App passwords" en la misma página
# 4. Seleccionar "Mail" y "Other (Custom name)"
# 5. Copiar la contraseña generada (16 caracteres sin espacios)
# 6. Usar esa contraseña en SMTP_PASSWORD
