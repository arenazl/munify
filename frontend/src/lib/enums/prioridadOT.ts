// Alias de compatibilidad. El SSoT de prioridad vive en `./prioridad`.
// Este archivo se mantiene para no romper imports existentes (OrdenesTrabajo,
// printOrdenTrabajo). En código nuevo importar directamente desde `./prioridad`.
export * from './prioridad';
